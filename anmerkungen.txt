
Änderungen:
- paar unnötige Catches entfernt
- Ticker-Fenster um Spieler-Infos erweitert
- PlayerAgent sucht nun direkt in den Player-eigenen Shares
- Logging über Proxy funktioniert soweit

Probleme:
- Keine Ahnung wie die Konfig. File für das Logging eingebunden wird
- Habe mich zwar grdsl. an die Vorlagen im Skript gehalten, kann evtl. trotzdem noch z.T. unsauber sein (z.B. wird dem Acc.Manager momentan sein eigener Proxy übergeben, damit der PlayerAgent diesen erhält...) 